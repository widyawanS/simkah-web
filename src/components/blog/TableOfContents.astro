---
---
<div class="toc-container brutal-card my-8 overflow-hidden">
  <button 
    id="toc-toggle"
    class="w-full text-left p-5 flex items-center justify-between hover:bg-brutal-primary/10 transition-colors"
  >
    <div class="flex items-center gap-3">
        <div class="w-8 h-8 rounded-brutal bg-brutal-primary border-2 border-brutal-border flex items-center justify-center shadow-brutal-sm">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-brutal-text" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h7" />
            </svg>
        </div>
        <span class="text-sm font-extrabold text-brutal-text uppercase tracking-wider">Daftar Isi</span>
    </div>
    <svg id="toc-chevron" xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-brutal-text-muted transition-transform duration-300" fill="none" viewBox="0 0 24 24" stroke="currentColor">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
    </svg>
  </button>
  
  <nav id="toc" class="hidden px-6 pb-6">
    <ul id="toc-list" class="space-y-3 text-sm text-brutal-text-muted border-l-2 border-brutal-border ml-2 pl-4">
    </ul>
  </nav>
</div>

<script is:inline>
  function setupTOC() {
    const tocList = document.getElementById('toc-list');
    const tocNav = document.getElementById('toc');
    const tocToggle = document.getElementById('toc-toggle');
    const tocChevron = document.getElementById('toc-chevron');
    const article = document.querySelector('article');
    
    if (!article || !tocList || !tocNav || !tocToggle) return;

    // Clear previous items
    tocList.innerHTML = '';

    const headings = article.querySelectorAll('h2, h3');
    if (headings.length === 0) {
      const container = document.querySelector('.toc-container');
      if (container) container.style.display = 'none';
      return;
    }

    headings.forEach((heading, index) => {
      const id = heading.id || `heading-${index}`;
      heading.id = id;

      const li = document.createElement('li');
      const a = document.createElement('a');
      a.href = '#' + id;
      a.textContent = heading.textContent;
      a.className = 'block hover:text-brand-primary transition-colors py-1 font-bold';
      
      if (heading.tagName === 'H3') {
        li.className = 'pl-4 text-xs opacity-80';
      }

      li.appendChild(a);
      tocList.appendChild(li);
    });

    // Toggle logic (default is hidden as requested)
    tocToggle.onclick = function() {
        const isHidden = tocNav.classList.contains('hidden');
        if (isHidden) {
            tocNav.classList.remove('hidden');
            tocChevron.classList.add('rotate-180');
        } else {
            tocNav.classList.add('hidden');
            tocChevron.classList.remove('rotate-180');
        }
    };
  }

  // Run on load
  setupTOC();
  
  // Handle view transitions
  document.addEventListener('astro:after-swap', setupTOC);
</script>
